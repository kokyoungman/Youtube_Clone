extends layouts/main
include mixins/videoBlock

block content
	.search_header
		h3 검색어 : #{searchingBy}
	.search__videos
		each video in videos
			+videoBlock({
				title: video.title,
				views: video.views,
				videoFile: video.videoFile
			})